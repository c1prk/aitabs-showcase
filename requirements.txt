# Audio (Phase 2)
librosa>=0.11.0
demucs>=4.0.0
basic-pitch>=0.4.0
pretty_midi>=0.2.10
soundfile>=0.12.0
pyloudnorm>=0.1.1  # MIT — ITU-R BS.1770 loudness normalization (preprocess front-end)

# Live mode (scripts/live_monitor.py) — needs system PortAudio (libportaudio2)
sounddevice>=0.4.6

# Vision (Phase 3)
mediapipe>=0.10.0
opencv-python>=4.8.0
Pillow>=10.0.0

# ML
torch>=2.1.0
torchvision>=0.16.0
torchaudio>=2.1.0
torchcodec>=0.1.0
transformers>=4.40.0
piano_transcription_inference>=0.0.6  # Kong (2021) CRNN — Apache 2.0 code, CC-BY-4.0 weights

# Export (notebook validation only)
PyGuitarPro>=0.6.1

# Notebooks & eval
numpy>=1.26.0,<2
matplotlib>=3.8.0
scipy>=1.12.0
tqdm>=4.66.0
jupyter>=1.0.0
ipykernel>=6.29.0
pytest>=8.0.0
